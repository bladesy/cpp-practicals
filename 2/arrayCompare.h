#ifndef ARRAY_COMPARE
#define ARRAY_COMPARE

/* Returns 1 if both arrays of the same specified size have all the same
elements of the same multiplicities, and 0 otherwise */
int sameElements(int *firstArray, int *secondArray, int size);

/* Sort array of specfied size in ascending order */
int bubbleSort(int *array, int size);

#endif
